# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BSDevLibs' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  project './BSDevLibs.xcodeproj'
  pod 'Alamofire', '~> 4.8'
  # Pods for BSDevLibs

end

target 'BSDevLibsExamples' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  # Pods for BSDevLibsExamples
  
end
